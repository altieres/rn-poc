import React from 'react';
import { DefaultContainer, DefaultText } from './components';

export default function Player() {
  return (
    <DefaultContainer>
      <DefaultText>Open up App.tsx to start working on your app!</DefaultText>
    </DefaultContainer>
  );
}
